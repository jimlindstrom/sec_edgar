1. Cause some of the tests (maybe the QuarterlyReport) iterate over multiple stocks, so that they catch things before the create_training script does.
2. Refactor a common parent class for the asset/liab/equity classifiers
